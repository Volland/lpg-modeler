// Generated by lpg-modeler. Target: gql.
// Model: social <https://example.org/vocab/social#>
//
// Element types follow the GQL graph type grammar of ISO/IEC 39075. Engines differ
// on the statement that installs a graph type: Neo4j writes the same body after
// ALTER CURRENT GRAPH TYPE SET. Adjust the wrapper, not the element types.
//
// Abstract types (Party) get no element type.
// They exist only as implied labels, which is how the hierarchy is carried.

CREATE GRAPH TYPE socialGraphType AS {
  (carType: Car => {
    vin :: STRING NOT NULL IS NODE KEY,
    seats :: INTEGER
  }),
  (companyType: Company => :Party {
    vat :: STRING,
    id :: STRING NOT NULL IS NODE KEY
  }),
  (personType: Person => :Party {
    email :: STRING NOT NULL IS NODE UNIQUE,
    born :: DATE,
    createdAt :: LOCAL DATETIME NOT NULL,
    id :: STRING NOT NULL IS NODE KEY
  }),
  (:Person)-[knowsType: KNOWS => {
    since :: DATE
  }]->(:Person),
  (:Person)-[likesType: LIKES =>]->(:Company),
  (:Party)-[ownsType: OWNS => {
    since :: DATE
  }]->(:Car)
}
